spring.application.name=GitHub-actions-test1

# Prefer an explicit MongoDB URI from the environment. Keep a safe local fallback so the app can boot
# in CI/local environments that do not currently provide Atlas credentials.
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017}
